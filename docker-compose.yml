services:

  # ──────────────────────────────────────────
  # MySQL Database
  # ──────────────────────────────────────────
  mysql:
    image: mysql:8.1
    container_name: ticketnest-db
    # restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - ${MYSQL_PORT}:3306
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - ticketnest-net

  # ──────────────────────────────────────────
  # Spring Boot Backend
  # ──────────────────────────────────────────
  backend:
    build:
      context: ../ticketnest-backend   # sibling repo — both must be cloned side by side
      dockerfile: Dockerfile
    container_name: ticketnest-backend
    # restart: unless-stopped
    environment:
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "${BACKEND_PORT}:8080"                    # useful for Swagger UI: http://localhost:8080/swagger
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - ticketnest-net

  # ──────────────────────────────────────────
  # React + Vite Frontend (served by Nginx)
  # ──────────────────────────────────────────
  frontend:
    build:
      context: .                       # this repo
      dockerfile: Dockerfile
    container_name: ticketnest-frontend
    # restart: unless-stopped
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - backend
    networks:
      - ticketnest-net

networks:
  ticketnest-net:
    driver: bridge

volumes:
  mysql_data:
